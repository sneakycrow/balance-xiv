export enum RoleEnum {
  // lowercase because we compare the url to the enum property when we do role in RoleEnum
  tanks = "tanks",
  healers = "healers",
  melee = "melee",
  ranged = "ranged",
  casters = "casters",
}
